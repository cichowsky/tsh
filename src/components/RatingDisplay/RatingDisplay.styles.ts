import styled from 'styled-components';

export const StyledRatingDisplay = styled.div`
  display: inline-flex;
  flex-shrink: 0;
  gap: 0.8rem;
  margin: 1.6rem 0;

  & > img {
    width: 1.6rem;
    height: 1.6rem;
  }
`;
